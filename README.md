# SentencePiece Framework Bundle

This archive is intended for `https://github.com/smdesai/SegmentText`.  
Unzip its contents into `SegmentText/Frameworks` so the SegmentText project can locate the prebuilt SentencePiece frameworks.
